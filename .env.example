# Copy to .env only if it does not already exist, then add your server sk- key.
# Keep .env private and ignored by Git; never commit or share real API keys.
# Running examples contacts production and may consume credits. Tests use fake keys.
JANUARY_API_KEY=
